$(".book").html (

	"<%= j render partial: 'bookings/submit' %>"
	
);

$(".date_start").html (

	"<%= j render partial: 'bookings/date_start' %>"
	
);

$(".date_end").html (

	"<%= j render partial: 'bookings/date_end' %>"
	
);